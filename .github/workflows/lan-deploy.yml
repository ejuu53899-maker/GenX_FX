name: GENX LAN Controller

on:
  workflow_dispatch:
  push:
    branches: [ main, feature/* ]

jobs:
  lan-health:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Configure GENX Environment
        env:
          LAN_HOST: ${{ vars.GENX_LAN_HOST }}
          LAN_PORT: ${{ vars.GENX_LAN_PORT }}
          LAN_TOKEN: ${{ secrets.GENX_LAN_TOKEN }}
        run: |
          echo "GENX LAN target configured"
          echo "Host: ${LAN_HOST:-192.168.1.100}"
          echo "Port: ${LAN_PORT:-8080}"
          ./scripts/lan_health.sh
